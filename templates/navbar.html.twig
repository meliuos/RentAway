<nav class="navbar navbar-expand-lg fixed-top bg-light navbar-light">
    <div class="container-fluid">
        <a class="navbar-brand nav-logo" href="{{path('HomeController') }}">RentAway</a>
        <button
                class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent"
                aria-expanded="false"
                aria-label="Toggle navigation"
        ><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item">
                    {% if app.session.get('username') %}
                    <a class="nav-link mx-2" href="{{ path('app_rent') }}">Rent your house</a>
                    {% else %}
                    <a class="nav-link mx-2" href={{ path('login') }}>Rent your house</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if app.session.get('admin') == "1" %}
                    <a class="nav-link mx-2" href="{{ path('admin') }}">Admin Dashboard</a>
                    {% endif %}
                </li>
                <li class="nav-item">
                    {% if app.session.get('username') %}
                    <a class="nav-link mx-2" href="{{path("app_manage_post")}}">Manage your posts</a>
                    {% else %}
                    <a class="nav-link mx-2" href="{{path('login')}}">Manage your posts</a>
                    {% endif %}
                </li>

                {% if app.session.get('username') %}
                <li class="nav-item ms-3">
                    <a class="btn btn-black btn-rounded" href="{{ path('logout') }}">Logout</a>
                </li>
                {% else %}
                <li class="nav-item ms-3">
                    <a class="btn btn-black btn-rounded" href="{{ path('login') }}">Login</a>
                </li>
                <li class="nav-item ms-3">
                    <a class="btn btn-black btn-rounded" href="{{ path('signup') }}">Sign up</a>
                </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>