<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand nav-logo" href="#">RentAway</a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    {% if session.get('username') %}
                    <a class="nav-link" href="/post">Rent your House</a>
                    {% else %}
                <a class="nav-link" href={{ path('login') }}>Rent your House</a>
                    {% endif %}
                </li>
                {% if session.get('admin') == "1" %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin') }}">Admin Dashboard</a>
                </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{path("edit")}}">Manage your posts</a>
                </li>
            </ul>
            <ul class="navbar-nav ms-auto">
                {% if session.get('username') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('logout') }}">Logout</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href={{ path('login') }}>Login</a>
                         <a class="nav-link" href={{ path('signup') }}>Sign Up</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
